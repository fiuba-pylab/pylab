n = input('Ingrese un número decimal: ')
binario = ""
if n == 0:
    print("0")
while n > 0:
    """ Si el n es par, se agrega un 0 al binario. Si es impar, se agrega un 1"""
    binario = str(n % 2) + binario
    if n % 2:
        n -= 1
    n /= 2
print(f'El número en binario es: {binario}')
