#cadenas son inputs, adaptado para testing
cadena_a = 2 
cadena_b = 2
cadena_c = 4

a = float(cadena_a)
b = float(cadena_b)
c = float(cadena_c)

determinante = b ** 2 - 4.0 * a * c

raiz_1 = (- b + math.sqrt(determinante)) / (2.0 * a)
raiz_2 = (- b - math.sqrt(determinante)) / (2.0 * a)

print(f'\n Una solución es {raiz_1} otra es {raiz_2}')